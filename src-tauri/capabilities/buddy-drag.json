{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "buddy-drag",
  "description": "Allow only the Buddy surface to hand an intentional drag to the native window manager",
  "windows": ["buddy"],
  "permissions": ["core:window:allow-start-dragging"]
}
